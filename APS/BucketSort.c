#include <stdio.h>
#include <stdlib.h>/*
void BucketSort(int a[], int n)
{  
    int i, j;  
    int count[n]; 
    for (i = 0; i < n; i++)
        count[i] = 0;
 
    for (i = 0; i < n; i++)
        (count[a[i]])++;
 
    for (i = 0, j = 0; i < n; i++)  
        for(; count[i] > 0; (count[i])--)
            a[j++] = i;
}   
int main()
{
	int n = 3000, i, x, a[n];
	for(x = 0; x < n; x++)
		a[x] = rand();
   
    BucketSort(a, n); 
    for (i = 0; i < n; i++)
        printf("%d ", a[i]);   
    printf("\n");     
    return 0;
}*/
